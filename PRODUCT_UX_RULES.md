# 제품 핵심 설계 규칙 (Product & UX Rules)

QR오더 전 제품의 화면을 **확장·수정할 때 판단 기준**이 되는 규칙 문서입니다.
디자이너는 화면을 그리기 전에, 개발자는 구현 전에 이 문서로 대조합니다.

| 항목 | 내용 |
|---|---|
| 버전 | v1.0 (2026-07-31) |
| 적용 범위 | 사장님사이트(`index.html`), 손님 화면(`customer.html`), 야외형 손님 화면(`outdoor-customer.html`) |
| 근거 문서 | `DESIGN_DECISIONS.md`, `SPEC.md`, `STATES.md`, `UX검증리포트_outdoor-customer.md` |
| 규칙 위반 처리 | Top 5(P0) 위반은 리뷰 반려. 그 외는 예외 사유를 PR/코멘트에 남기면 통과 |

**핵심 사용자 두 축** — 이 규칙 전체가 이 두 사용자에게서 나옵니다.
- **사장님(50~60대, PC)** — 노안·저시력 고려, 실수 복구 가능성이 최우선. 화면은 데스크탑 최적화 + 로그인.
- **손님(전 연령, 모바일, 비로그인)** — 처음 쓰고 다시 안 볼 수도 있는 1회성 사용자. 학습을 요구하면 안 되고, 야외형은 서서 한 손으로 씁니다.

---

## Top 5 — 이것만은 반드시

| # | 규칙 | 한 줄 기준 | 상세 |
|---|---|---|---|
| 1 | **화면당 핵심 액션은 1개** | 이 화면에서 사용자가 눌러야 할 Primary 버튼을 하나로 말할 수 없으면 화면을 쪼갠다 | [L-01](#l-01-화면당-핵심-액션은-1개) |
| 2 | **CTA 라벨은 "누른 다음 일어날 일"을 말한다** | 특히 시간·수령 방식이 갈리는 액션(즉시 포장 / 포장 예약)은 라벨에 못박는다 | [B-01](#b-01-cta-라벨은-동사--대상--결과다), [B-02](#b-02-시간과-수령-방식은-라벨에-못박는다) |
| 3 | **되돌릴 수 없는 행동 앞에는 영향을 보여주는 확인을 둔다** | "정말 하시겠어요?"가 아니라 "무엇이 어떻게 바뀌는지"를 보여준다 | [B-06](#b-06-되돌릴-수-없는-행동은-영향을-보여주고-확인받는다) |
| 4 | **문구는 능동·긍정, 다음 행동을 짚어준다** | 상태를 통보하지 말고 행동을 지목한다. 꼭 해야 하면 "~해 주세요", 해도 되고 안 해도 되면 "~할 수 있어요" | [C-01](#c-01-능동긍정으로-쓰고-다음-행동을-지목한다), [C-02](#c-02-명령형과-가능형을-구분한다) |
| 5 | **물리 규격은 타협하지 않는다** | 본문 13px 이상, 터치 타깃 44px 이상, 텍스트 대비 WCAG AA 이상 | [L-09](#l-09-물리-규격-폰트터치대비) |

---

# 원칙 1. UX & 레이아웃

## L-01. 화면당 핵심 액션은 1개

**상세** — 한 화면에는 Primary 버튼이 하나만 존재합니다. 사용자가 "이 화면에서 내가 할 일"을 1초 안에 답할 수 있어야 합니다. 보조 액션은 Secondary/Ghost로 위계를 낮추고, Primary가 두 개 필요하다고 느껴지면 그건 화면이 두 개라는 신호입니다.
단, **서로 배타적인 선택지**(먹고가기 / 포장하기)는 예외 — 같은 위계의 선택 카드 2~3개는 허용하되, 그 화면은 "선택 화면"이므로 다른 Primary를 추가하지 않습니다.

- **Do** — 장바구니 화면: 하단 고정 `결제하기` 하나. 쿠폰 적용·수량 변경·삭제는 인라인 컨트롤로 처리.
- **Don't** — 장바구니 하단에 `결제하기`와 `메뉴 더 담기`를 같은 크기·같은 색으로 나란히 두기.

## L-02. 정보 위계는 3단으로 고정한다

**상세** — 모든 화면의 요소를 세 등급으로 분류하고, 등급별 시각 처리를 통일합니다.
1. **결정 정보** — 사용자가 이 화면에서 판단하는 데 필요한 값(메뉴명, 가격, 수령 방식, 대기 시간). 가장 크고 진하게.
2. **보조 정보** — 판단을 돕지만 없어도 되는 값(원산지, 안내 문구, 매장명). 13px, `--ink-3` 이하로.
3. **장식** — 구분선, 비활성 아이콘. 의미를 갖지 않으므로 대비 규정 예외.

같은 카드 안에서 무엇을 크게 볼지는 **화면의 목적**이 정합니다. 야외형 홈에서 대표메뉴를 크게, 매장명을 작게 뒤집은 처리가 좋은 예입니다.

- **Do** — 품절 임박 메뉴: 남은 수량을 결정 정보로 승격해 강조.
- **Don't** — 매장명·카테고리·해시태그·거리·평점을 모두 같은 14px로 나열해 무엇을 봐야 할지 모르게 하기.

## L-03. 액션은 화면 하단에 고정하고, 판단 근거를 같이 붙인다

**상세** — 모바일 화면의 Primary 액션은 스크롤과 무관하게 하단 고정(sticky)입니다. 금액이 걸린 액션이면 버튼 바로 위 또는 버튼 안에 **최종 결제 금액**을 함께 표시합니다. 사용자가 위로 스크롤해서 금액을 다시 확인하게 만들면 안 됩니다.

- **Do** — `12,400원 결제하기` (버튼 안에 금액 포함)
- **Don't** — 하단 버튼은 `결제하기`만, 금액은 화면 최상단 요약에만 있어 스크롤이 필요한 구조.

## L-04. 목표 도달은 3단계, 뒤로가기는 진입 출처를 따른다

**상세** — 손님이 주문에 도달하는 경로는 최대 3탭(홈 → 매장 → 메뉴 담기)을 넘지 않습니다. 단계를 늘려야 하는 요구가 생기면 단계를 추가하기보다 기존 단계를 병합할 방법을 먼저 검토합니다.
뒤로가기는 **어디서 들어왔는지**를 기억해 그 자리로 돌려보냅니다. 동일 화면인데 진입 경로에 따라 뒤로가기 목적지가 달라야 한다면 진입 출처를 상태로 저장합니다. (현재 야외형 장바구니의 뒤로가기 불일치는 이 규칙 위반 케이스 — `UX검증리포트` 9번)

- **Do** — 홈의 통합 장바구니로 진입했으면 뒤로가기는 홈, 매장에서 진입했으면 매장.
- **Don't** — 진입 경로와 무관하게 항상 `go('store')`로 고정.

## L-05. 탐색 축(필터)에는 반드시 라벨을 붙인다

**상세** — 필터·정렬·세그먼트 등 "무엇을 거르는 축"은 운영자가 값을 정하는 구조이므로, 축 이름을 **데이터로 받아** 노출합니다. 라벨이 설정되지 않은 축은 화면에 그리지 않습니다(숨김). 코드 변수명도 의미를 특정하지 않는 중립 명명(`filter1`/`filter2`)을 씁니다. 하드코딩된 "분류/구역"은 다른 행사에서 즉시 거짓말이 됩니다.

- **Do** — `메뉴 종류` 라벨 + 칩 줄, `구역` 라벨 + 칩 줄. 축이 1개만 설정된 행사면 한 줄만 노출.
- **Don't** — 라벨 없이 `전체 / 전체`로 시작하는 칩 두 줄.

## L-06. 예외 상태 6종은 화면 설계의 필수 산출물이다

**상세** — 새 화면을 설계하면 happy path와 함께 아래 상태를 **같은 PR에서** 함께 정의합니다. 하나라도 빠지면 화면 설계 미완으로 봅니다.

| 상태 | 필수 요소 |
|---|---|
| 로딩 | 스켈레톤 또는 인디케이터. 3초 초과 시 안내 문구 |
| 비어 있음 | 왜 비었는지 + 다음 행동을 지목하는 버튼 |
| 검색 결과 없음 | 입력값 유지 + 조건 완화 제안 |
| 품절 / 판매 불가 | 상태 배지(텍스트 동반) + 담기 차단 이유 |
| 실패 (결제·전송) | 무슨 일이 있었는지 + 재시도 경로 + 돈이 빠졌는지 여부 |
| 만료 (세션·QR) | 재진입 방법. 장바구니 보존 여부 명시 |

## L-07. 카드는 뉴트럴하게, 컬러는 1~2개로

**상세** — 상자는 1px 옅은 뉴트럴 테두리(`var(--line)`) + 둥근 모서리 + 소프트 섀도우로만 구성합니다. 진한 테두리의 단색 박스, **카드 좌측 컬러 액센트 레일(`border-left`)** 은 금지 — 자동 생성된 티가 나서 실사용 앱의 톤을 해칩니다. 강조는 아이콘·볼드 텍스트·태그로 처리합니다.
메인 컬러는 1~2개. 강조 상자(연한 배경 블록)는 **결제 차단·데이터 손실**처럼 반드시 인지해야 하는 정보에만 씁니다. 레퍼런스 톤은 ZIGZAG·오늘의집류 클린 카드.

- **Do** — 흰 카드 + `--line` 테두리 + 아이콘/볼드로 강조.
- **Don't** — `border-left: 4px solid orange` 카드, 색만 다른 강조 박스를 화면마다 뿌리기.

## L-08. 아이콘은 하나의 라인 SVG 세트로 통일한다

**상세** — OS 기본 이모지(🏪 🧾 🎟️ 등)는 UI 아이콘으로 사용 금지입니다. 플랫폼마다 렌더가 달라 정렬·크기 통제가 안 되고, 완성도가 들쭉날쭉해집니다. 자주 쓰는 아이콘(매장·위치·대기·픽업·알림·영수증·쿠폰·뒤로·체크 등)을 한 세트의 인라인 라인 SVG로 정의해 재사용합니다. 한 화면 안에 SVG와 이모지가 섞이는 것이 가장 나쁜 상태입니다.

- **Do** — 단일 세트 인라인 SVG(Lucide/Tabler류) + `aria-label`.
- **Don't** — 헤더는 SVG인데 리스트는 🥡 🛎️ 를 쓰는 혼재.

## L-09. 물리 규격 (폰트·터치·대비)

**상세** — 사용자 특성상 아래 수치는 디자인 취향의 문제가 아니라 **동작 요건**입니다.

| 항목 | 기준 | 근거 |
|---|---|---|
| 본문 텍스트 | 최소 13px (12px 이하 금지) | 사장님 노안 (`D-016`) |
| 보조 설명문 | 13px, `--ink-3` 이상 대비 | 동일 |
| 터치 타깃 | 44 x 44px 이상 (시각 크기는 유지하고 투명 히트영역 확대 허용) | 야외에서 서서 한 손 사용 |
| 텍스트 대비 | WCAG AA 이상. 작은 글자는 4.5:1 | `--ink4`(1.9:1)는 장식 전용 |
| 타입 스케일 | 12/14/16/20/24/32 6단으로 수렴. 반픽셀(13.5px) 금지 | 유지보수 |
| 폰트 굵기 | 400/700(강조 800). 600/900 혼용 금지 | 동일 |
| 색 토큰 | 반복 hex는 의미 토큰으로(`--danger` 등). 이름과 실제 색이 다른 토큰 금지 | `UX검증리포트` 12번 |

## L-10. 의미는 색만으로 전달하지 않는다

**상세** — 상태(품절·실패·예약)를 색으로만 구분하지 않고 항상 텍스트 라벨 또는 아이콘을 동반합니다. 배지는 색 + 텍스트가 기본 조합입니다.

- **Do** — 회색 배지 안에 `품절`, 빨간 배지 안에 `결제 실패`.
- **Don't** — 카드 테두리만 빨갛게 바꿔 "문제 있음"을 표현.

---

# 원칙 2. 인터랙션 & 버튼 규칙

## B-01. CTA 라벨은 [동사 + 대상 + 결과]다

**상세** — 라벨만 읽고 **누른 직후 무엇이 벌어지는지** 예측할 수 있어야 합니다. `확인`, `완료`, `다음`처럼 결과를 감추는 라벨은 확인 모달의 중립 버튼에만 허용하고, 화면의 Primary에는 쓰지 않습니다.

- **Do** — `12,400원 결제하기` / `장바구니에 담기` / `주문 취소하기` / `매장 그룹 저장하기`
- **Don't** — `확인` / `완료` / `OK` / `제출`

## B-02. 시간과 수령 방식은 라벨에 못박는다

**상세** — "언제 받는지"가 갈리는 액션은 라벨 자체에서 구분합니다. 같은 화면에 두 선택지가 함께 있으면, 시간 정보를 라벨 또는 바로 아래 보조문에 붙입니다. 예약형은 **시간을 고르는 단계가 있다는 사실**을 라벨에서 예고합니다.

| 상황 | Do | Don't |
|---|---|---|
| 지금 조리해 바로 받음 | `즉시 포장으로 주문하기` | `주문하기` |
| 시간을 지정해 나중에 받음 | `포장 예약 시간 고르기` → 다음 단계에서 `19:30 예약 주문하기` | `예약` |
| 배타 선택 세그먼트 | `즉시 포장 / 포장 예약` 세그 + 하단에 선택 결과 반영된 Primary 1개 | 세그 없이 버튼 두 개를 나란히 |

**위계 주의** — 브랜드·운영자가 설정하는 값(서비스 유형: 포장 / 자리로 받기)과 손님이 고르는 값(즉시 / 예약)은 다른 층입니다. 손님이 바꿀 수 없는 값은 선택 UI로 그리지 않고, 소제목 한 줄로 사실만 알립니다.

## B-03. 버튼 위계는 3단으로만 존재한다

**상세** — Primary(채움) / Secondary(테두리) / Ghost(텍스트)로 고정하고, 화면당 Primary는 1개(L-01). 파괴적 액션(삭제·취소·환불)은 별도 Danger 스타일이지만, 확인 모달 안에서만 Primary 위치를 차지할 수 있습니다.

- **Do** — 모달: `취소`(Ghost) + `삭제하기`(Danger). 파괴적 버튼은 하나만.
- **Don't** — Primary 두 개, 또는 삭제 버튼을 목록 화면에서 채운 빨간 버튼으로 상시 노출.

## B-04. 모든 버튼은 5개 상태를 갖는다

**상세** — 기본 / 눌림(press) / 로딩 / 비활성 / 완료. 특히 **로딩 상태에서 버튼은 스스로 잠깁니다** — 결제·주문 전송처럼 중복 실행이 사고가 되는 액션은 첫 탭 즉시 비활성 + 스피너로 전환합니다.
반복 탭이 예상되는 액션(직원 호출)은 서버 측 디바운스를 함께 둡니다. 같은 테이블·같은 메시지 30초 내 재호출은 무시(`D-010`).

- **Do** — `결제하기` 탭 → 즉시 잠금 + `결제 중이에요` → 성공 화면 전환.
- **Don't** — 응답 대기 중에도 버튼이 계속 눌리는 상태로 방치.

## B-05. 비활성 버튼은 이유를 함께 말한다

**상세** — 회색으로 죽은 버튼만 보여주는 것은 사용자를 막다른 길에 세우는 것입니다. 비활성이면 **무엇을 채워야 활성화되는지**를 버튼 근처에 노출합니다(가능하면 부족한 항목을 직접 지목). 이때 문구는 명령형(`~해 주세요`)입니다.

- **Do** — 비활성 `결제하기` + 아래 `연락처를 입력해 주세요`
- **Don't** — 비활성 `결제하기` 만 두고 이유 없음 / 툴팁에만 숨김

## B-06. 되돌릴 수 없는 행동은 영향을 보여주고 확인받는다

**상세** — 확인 모달의 목적은 재확인이 아니라 **영향 고지**입니다. 아래 케이스는 모달이 필수이고, 모달 본문에는 "무엇이 어떻게 바뀌는지"를 구체적으로 씁니다.

| 케이스 | 모달에 반드시 담을 것 | 근거 |
|---|---|---|
| 주문 채널 변경 | 손님 화면에서 어떤 QR·주문 유형이 사라지는지 | `D-012` |
| 가격 30% 이상 변경 | 변경 전 → 후 금액 대조 (오타 방지) | `D-013` |
| 다른 매장 메뉴 담기 | 기존 장바구니가 교체된다는 사실 | 야외형 현행 |
| 마스터 데이터 삭제 | 진행 중 주문 영향 여부. 복구 경로(CS) | `D-015` |
| 결제·환불 실행 | 금액, 수단, 되돌릴 수 없음 | — |

- **Do** — 제목 `포장만 받도록 바꿀까요?` / 본문 `테이블 QR로는 주문을 받을 수 없게 돼요. 미수락 포장 예약 2건은 먼저 처리해 주세요.` / 버튼 `그대로 두기` + `포장만 받기`
- **Don't** — 제목 `경고` / 본문 `정말 변경하시겠습니까?` / 버튼 `예` + `아니오`

## B-07. 피드백은 행동이 일어난 자리에서 준다

**상세** — 결과를 알리는 위치를 액션의 성격에 맞춥니다.

| 성격 | 피드백 방식 |
|---|---|
| 되돌릴 수 있는 소액 변경(담기, 수량) | 인라인 즉시 반영 + 짧은 토스트(2초) |
| 입력값 오류 | 해당 입력 필드 아래 인라인 (모달·토스트 금지) |
| 화면 맥락이 바뀌는 결과(결제 완료) | 전용 완료 화면 |
| 추가 판단이 필요한 정보(약관, 영수증, 공지 상세) | 바텀시트 |

**외부 링크로 도망가지 않기** — 공지·약관 같은 필수 정보는 새 탭 대신 인앱 바텀시트로 띄우고, **시트를 실제로 연 시점**에 읽음 처리합니다. 열지도 않았는데 읽음이 되는 구조는 금지(`UX검증리포트` 3번).

## B-08. 접근성은 구현 요건이다

**상세** — 아래는 리뷰에서 확인하는 항목입니다.
- 누를 수 있는 것은 실제 `<button>`/`<a>`/`<input>`. `div onclick` 금지. 토글은 `aria-pressed`, 선택은 `role="radio"`.
- 공통 포커스 링: `:focus-visible{ outline: 2px solid var(--accent); outline-offset: 2px }`
- 아이콘 단독 버튼에는 `aria-label` 필수.
- `@media (prefers-reduced-motion: reduce)` 로 무한 펄스·자동 롤링·등장 모션을 정지.

- **Do** — `<button class="opt" aria-pressed="true">라지</button>`
- **Don't** — `<div class="opt" onclick="pick()">라지</div>`

## B-09. 같은 기능의 접점은 한 곳으로 모은다

**상세** — 동일 기능이 흐름상 두 번 등장하면 사용자는 "어디서 적용된 건지" 추적을 잃습니다. 접점을 하나로 통합하거나, 불가피하면 **선택 상태를 두 화면이 동일한 배지로 공유**합니다. (쿠폰 입력이 주문확인·결제시트 두 곳에 있는 현행 구조가 이 케이스 — `UX검증리포트` 11번)

- **Do** — 쿠폰은 결제 직전 한 곳에서만 적용, 주문 요약에는 `쿠폰 2,000원 적용` 결과만 표기.
- **Don't** — 두 화면에서 각각 입력받고 어느 쪽이 최종인지 불명확.

## B-10. 자동 판정보다 사용자의 명시적 의사가 강하다

**상세** — 상태 충돌 시 우선순위는 `사장님 명시적 의사 > 시스템 자동 판정`입니다(`D-003`). 완료·처리 트리거도 명시적 버튼 클릭으로 잡고, 팝업 닫기·자동 만료를 완료로 해석하지 않습니다(`D-009`). 화면에서 자동으로 바꾸는 값은 사용자 의도를 덮어쓰지 않는 것만 허용합니다(`D-005`).

- **Do** — 직원 호출 처리 완료는 `완료` 버튼 클릭으로만. 10분 경과 시 알림 + 배지.
- **Don't** — 팝업이 닫혔으니 처리된 것으로 간주.

---

# 원칙 3. 톤앤매너 & 카피라이팅

## C-01. 능동·긍정으로 쓰고, 다음 행동을 지목한다

**상세** — 상태를 통보하는 수동·부정 표현 대신, 사용자가 할 행동을 짚습니다. 카운트성 라벨도 행동 유도형으로 바꿉니다. 대상이 50~60대 사장님이라 차가운 통보체는 무력감을 줍니다.

| Don't | Do |
|---|---|
| `미확인 4건` | `4건 확인해 주세요` |
| `등록된 메뉴가 없어요` | `첫 메뉴를 등록해 주세요` |
| `조건에 맞는 결과가 없어요` | `다른 조건으로 한번 찾아봐 주세요` |
| `사용할 수 없는 쿠폰입니다` | `1만원 이상 주문에 쓸 수 있어요` |

빈 상태는 다음 액션을 직접 지목합니다 — `위 + 버튼을 눌러 추가해 주세요`.

## C-02. 명령형과 가능형을 구분한다

**상세** — 판단 기준은 하나입니다.
- **꼭 해야 다음으로 갈 수 있다** → `~해 주세요` (필수 입력, 잘못된 값, 비활성 해제 조건)
- **안 해도 되지만 하면 더 잘 쓴다** → `~할 수 있어요` (기능 소개, 검색·필터 힌트, 빈 상태의 부가 안내)

안내 문구가 압박으로 읽히면 안 됩니다.

- **Do** — `메뉴 이름으로도 찾을 수 있어요` / `연락처를 입력해 주세요`
- **Don't** — `더 정확하게 찾아 주세요` (선택 기능인데 명령형)

## C-03. 안전 정보는 부드럽게 만들지 않는다

**상세** — C-01의 예외입니다. 데이터 삭제·결제 차단·복구 불가처럼 **사용자가 반드시 인지해야 하는 사실**은 완곡하게 바꾸지 않고 그대로 씁니다.

- **Do** — `삭제하면 되돌릴 수 없어요` / `카드 결제는 취소 후 3~5일 뒤 환불돼요`
- **Don't** — `조금 아쉽지만 정리해 드릴게요` (위험을 숨기는 완곡)

동시에 공포·과장은 금지입니다. `주의!`, `경고!!`, 빨간 대문자, 느낌표 두 개 이상은 쓰지 않습니다.

## C-04. 한 문장으로 쓰고, 조건은 괄호로 붙인다

**상세** — 안내 문구는 한 문장입니다. 부가 조건은 문장을 쪼개거나 기호로 잇지 않고 괄호로 처리합니다. **불릿 기호(`·`, `ㅡ`) 를 문구에 쓰지 않습니다** — 나열 기호로 이어붙인 문구는 자동 생성된 느낌을 강하게 줍니다.

- **Do** — `포장 예약은 30분 뒤부터 고를 수 있어요 (매장 상황에 따라 조정될 수 있어요)`
- **Don't** — `포장 예약 · 30분 뒤부터 가능 · 매장 상황에 따라 변동`

## C-05. 표준 용어를 쓴다 (내부 용어 금지)

**상세** — 화면 라벨·안내·문서 모두 아래 표준 용어로 통일합니다. 코드 내부 변수·데이터 키는 굳이 바꾸지 않고 **표시 라벨만** 교체합니다.

| 쓰지 않음 | 표준 용어 |
|---|---|
| 가게 | 매장 |
| 매장식사 | 먹고가기 |
| 포장 | 포장하기 |
| 픽업 / 포장예약 / 미리주문 | 포장 예약 |
| 선결제 / 후결제 | 선불 / 후불 |
| 알림톡 | 카카오 알림톡 |
| 에이전트 | QR오더 프로그램 |
| 사용자페이지 | QR오더 메뉴판 |
| 식별번호 | 매장코드 |
| 메뉴그룹 | 메뉴 카테고리 |
| QR오더 점주 관리자 / 관리자 사이트 | 사장님사이트 |

`대표자`, `사장님`은 그대로 씁니다. 조직·권한 맥락의 `시스템 관리자`는 회사명(`(주)소프트먼트`)과 구분해 씁니다.

## C-06. 숫자·시간 표기 규칙

**상세**
- 금액 — 천 단위 콤마 + `원`. `12,400원`. 통화기호(₩)·소수점 금지.
- 시간(예약) — 24시간제 `19:30`. `오후 7시 반` 같은 구어 표기 금지.
- 상대시간(내역) — `방금 전`, `12분 전`, `3시간 전`. 24시간 초과는 절대 날짜(`7월 24일`)로 전환. 목록은 항상 최신순 정렬.
- 소요·대기 — 범위로 `15~20분`. 단일 확정값처럼 쓰지 않기.
- 영업일 — 매출·집계 문구에서 `오늘`은 영업일(03:00~다음날 02:59) 기준. 새벽 1시 매출은 `오늘`로 표기(`D-001`).

## C-07. 오류 메시지는 3요소를 갖춘다

**상세** — [무슨 일이 있었는지] + [원인 또는 돈/데이터 상태] + [다음 행동]. 에러 코드·영문 원문·스택은 사용자 화면에 노출하지 않습니다(필요 시 접어서 CS용으로).

- **Do** — `결제가 완료되지 않았어요. 카드 승인이 거절돼서 금액은 빠지지 않았어요. 다른 카드로 다시 시도해 주세요.`
- **Don't** — `Payment failed (ERR_PG_5023)` / `오류가 발생했습니다. 관리자에게 문의하세요.`

## C-08. 팝업·모달은 3단 구조로 쓴다

**상세**
- **제목** — 사용자가 지금 내리는 결정을 질문형으로. (`포장만 받도록 바꿀까요?`)
- **본문** — 결정의 영향을 구체적으로. 최대 2문장.
- **버튼** — 양쪽 모두 동사형. `예/아니오`, `확인/취소` 대신 `그대로 두기 / 포장만 받기`처럼 선택 결과를 라벨에 담습니다.

- **Don't** — 제목 `알림`, 본문 `변경하시겠습니까?`, 버튼 `확인` + `취소`

## C-09. 주체를 분명히 하고, 사과를 남발하지 않는다

**상세** — 시스템이 한 일과 사용자가 할 일을 섞지 않습니다. 손님 화면에서는 매장이 주체(`매장에서 준비를 시작했어요`), 사장님사이트에서는 시스템이 주체(`카카오 알림톡을 보냈어요`)입니다. `죄송합니다`는 실제로 서비스 귀책인 장애에만 한 번 씁니다.

- **Do** — `매장 두 곳에서 각각 준비하고, 준비되면 카카오 알림톡으로 알려드려요`
- **Don't** — `죄송하지만 매장별로 따로 처리되는 점 양해 부탁드립니다`

## C-10. 복잡한 규칙은 화면 흐름에 나눠 반복한다

**상세** — 한 번에 설명하기 어려운 비즈니스 규칙(통합 결제인데 매장별로 준비·픽업·알림이 따로 진행되는 구조 등)은 긴 안내문 하나로 처리하지 않고, **결제 직전 요약 / 완료 화면 / 주문 내역** 세 지점에서 짧게 반복합니다. 사용자가 그 정보를 필요로 하는 순간에 다시 보이게 하는 것이 핵심입니다.

---

# 신규·수정 화면 리뷰 체크리스트

디자인 전달 시 / PR 올릴 때 아래를 통과해야 합니다.

- [ ] 이 화면의 Primary 액션을 한 문장으로 말할 수 있다 (L-01)
- [ ] 요소가 결정 정보 / 보조 / 장식 3등급으로 분류돼 있다 (L-02)
- [ ] 금액이 걸린 액션에 금액이 함께 보인다 (L-03)
- [ ] 뒤로가기 목적지가 진입 출처와 일치한다 (L-04)
- [ ] 예외 상태 6종이 함께 정의됐다 (L-06)
- [ ] 좌측 컬러 레일·이모지·불릿(`·`)이 없다 (L-07, L-08, C-04)
- [ ] 본문 13px+ / 터치 44px+ / 대비 AA (L-09)
- [ ] CTA 라벨만 읽고 결과를 예측할 수 있다 (B-01)
- [ ] 즉시/예약 등 시간·수령 방식이 라벨에 드러난다 (B-02)
- [ ] 로딩 중 버튼이 잠기고, 비활성 사유가 노출된다 (B-04, B-05)
- [ ] 되돌릴 수 없는 행동에 영향 고지 모달이 있다 (B-06)
- [ ] 실제 `button` + `focus-visible` + `aria-label` + reduced-motion (B-08)
- [ ] 문구가 능동·긍정이고, 명령형/가능형이 구분돼 있다 (C-01, C-02)
- [ ] 표준 용어를 쓴다 (C-05)
- [ ] 오류 메시지에 다음 행동이 있다 (C-07)

---

# 예외 처리와 문서 갱신

- 규칙을 벗어나야 할 때는 화면을 먼저 바꾸지 말고 **이 문서에 예외를 추가**합니다. 예외 없이 어긴 화면은 리뷰에서 되돌립니다.
- 비자명한 결정은 `DESIGN_DECISIONS.md`에 `D-0nn`으로 기록하고, 이 문서에서 참조합니다.
- 목업을 수정하면 Notion 기능 명세에도 같은 내용을 반영합니다.

### [확인 필요] — 팀 확정이 필요한 추정 항목

아래는 기존 문서에 명문화돼 있지 않아 이번에 제안 수준으로 넣은 규칙입니다. 확정되면 이 절에서 지우고 본문만 남깁니다.

1. **L-04의 3탭 상한** — 현재 야외형 흐름(그룹 홈 → 매장 → 메뉴)에서 역산한 값. 내점형은 QR 진입이라 단계가 더 짧아 별도 기준이 필요할 수 있습니다.
2. **C-06 소요·대기 시간 범위 표기(`15~20분`)** — `조리시간-예상-기능명세.md`의 산출 방식과 표기를 맞춰야 합니다.
3. **B-04 로딩 잠금 시간 상한** — 몇 초 후 타임아웃 안내를 띄울지(현재 문서는 3초 기준만 L-06에 둠) 결정 필요.
4. **C-09의 주체 표기 규칙** — 손님 화면은 매장 주체, 사장님사이트는 시스템 주체로 제안. 카카오 알림톡 발신 명의와 함께 확정 필요.

### 변경 이력

| 일자 | 버전 | 내용 |
|---|---|---|
| 2026-07-31 | v1.0 | 최초 작성. `DESIGN_DECISIONS.md`(D-001·003·005·009·010·012·013·015·016), `UX검증리포트_outdoor-customer.md`, 확정 표준 용어, 톤 규칙(능동·긍정 / 가능형)을 근거로 통합 |
